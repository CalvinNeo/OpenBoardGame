# Store & Load 机制

在游戏开始前，可以选择是否自动保存游戏。如果开启自动保存，那么每次状态发生变化后，都需要落盘。落盘的位置是 `.data/${roomId}/${stateSeq}.save`。

用户现在可以选择 Load 之前自动保存的游戏。如果用户这么做了，你就要弹出一个对话框，列出所有可选的游戏。对于每个游戏，显示 roomId，并给出游戏的类型，以及参与的人。在用户选择完毕后，你会就创建这个 roomId 的房间。不过在此之前，你需要检查现在是否有相同 roomId 的房间。如果有，就报错。