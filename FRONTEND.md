# Frontend Requirements

在设计前端时：
- 应该尽量使用 Emoji 和颜色来方便阅读，例如：
  + 如果涉及到扑克牌花色，要用 Emoji 来显示，而不要用 H、C 这样的字母。
  + 如果涉及金钱、钻石，要用 Emoji 显示。如果游戏中有多种货币，则尝试用不同的 Emoji 来区分。
  + 对于可收集的物品，例如不同游戏中的香料、宝石、货物，如果它们有颜色属性，可以尝试用不同颜色的 Emoji 方块/圆形等来表示。

- 你需要支持 Help 和 Explain，你必须遵守 task40.md 的要求。
- 特别地，如果游戏中对一个东西，既有 emoji 图标，又有文字指代。那么在 Help 和 Explain 的时候，需要将它们都列出，如 Gold(💰) 这样。
- 你尽量避免引入 Clear Selection 按钮，改为点击周围空白区域就可以取消选择。对于弹出的对话框，支持使用 Esc 键退出。
- 所有和游戏内容无关的 UI，都使用英文。
- 涉及到 Checkbox / Input 等交互性组件和对应的说明文字的情况，除非放不下，否则将两者贴近在同行放置。
- 你的 UI 应该是美观、方便的，你不能设计需要打字输入 json 的 UI。
- 除非被明确指示这么做，你应该避免设计出来 layout 中的各个板块之间互相覆盖。
- 你不可以使用商用 UI 素材。
- 如果游戏日志之类的 UI 元素可能过长，要加上可以滚动的功能。
- 布局尽量不要太长，导致需要上下滚动。严格禁止太宽导致需要横向滚动的页面。
- 涉及到 UI 的改动，不需要运行逻辑测试。
- 包括不限于解密游戏，如果涉及到提供给玩家记录自己思路的笔记本组件，对于手机版，优先可以 dock 在右边，用户可以选择打开或者关闭。
- 严格禁止在移动端或者电脑端，文字或者图片或者 emoji 等内容溢出它的容器，或者和其他容器内或者相邻容器的内容重叠。如果出现这种情况，要考虑缩小内容尺寸（但也不能太小）或者扩大容器大小。如果还有问题，要选择积极的重新排版。
- 游戏中的图标、缩写等信息应提供简短说明，方便玩家直接了解含义，不必先进入 Help 或 Explain。电脑端鼠标悬停时显示说明；手机端点击后显示浮动提示，3 秒后自动消失。连续点击时显示最新的说明。

对于移动端布局，特别要注意：
- 可拖拽或平移的棋盘／地图必须在自身区域内接管触摸手势，阻止浏览器页面滚动与地图拖动竞争（例如在地图区域使用 `touch-action: none`）。区域外须保留页面正常滚动；指针移出地图后仍须能接收抬起或取消事件并正确结束拖动，且拖动结束不得误触星球／棋格的点击操作。
- 按钮排布需要紧凑。例如，如果可能，在一行中排放两个按钮，或者一个 inputbox 和一个按钮。
- 在排布紧凑的前提下，不同的组件之间需要有合理的 padding，不要贴在一起。
- 元素需要进行合理缩放，以适配手机的宽度。尽量避免超出手机的宽度。要避免界面缩放太小，导致对手机屏幕的利用率低下。
- 如果一行放不下，可以考虑换行放置。此时，需要考虑对齐和美观。
- 涉及到“选择”、“确认”某个 item 的时候，比如“购买”某张卡牌，可以实现为点选这卡牌，然后给这个卡牌加上半透明蒙版，蒙版上有 Buy 和 Cancel 两个选择。如果这个 item 本身显示尺寸太小，导致放不下这些选项，就可以蒙版一个足够大的上层容器。


# Coding style

你需要：
- 对于每一个新游戏，在 static/games/ 下新建一个 js 文件，放置主要逻辑。你只可以在必要的时候往 app.js 中添加代码，例如要实现的逻辑涉及修改全局；或者你需要注册一些东西到全局。
- 各游戏脚本中声明在浏览器全局作用域的函数和变量必须带有该游戏独有的前缀，避免不同游戏脚本加载后发生重名覆盖。例如使用 `isCaboActionAvailable`、`manilaSelectedBoat`，不要使用 `isActionAvailable`、`selectedBoat` 这类通用名称。封装在模块或 IIFE 私有作用域内、不会进入全局作用域的名称不受此限制。
- 只有修改了影响全局的代码才需要运行全局测试。修改了涉及某个模块的逻辑代码，只运行该模块的测试。修改 UI 不运行逻辑测试。

# Interaction Requirments

- 在“每一轮”结束后，例如揭开隐藏拍，展示最终结果，进行结算后，都需要暂停，让所有人可以去看一下当前的状态。所有玩家点击 Next Round 之后，游戏才会继续开始下一轮。
